import { projects } from "../data";

const Projects = () => {
  return (
    <div>
      <nav>Navbar</nav>
      <div>
        {
          projects.map(project => (
            
          ))
        }
      </div>
    </div>
  )
}

export default Projects;